import { _ as ShaderStore } from "./abstractEngine.pure-CkCw37qi.js";
import { a as morphTargetsVertex, c as clipPlaneVertexDeclaration, d as bakedVertexAnimationDeclaration, f as bonesDeclaration, i as instancesVertex, l as morphTargetsVertexDeclaration, n as bakedVertexAnimation, o as morphTargetsVertexGlobal, r as bonesVertex, s as instancesDeclaration, t as clipPlaneVertex, u as morphTargetsVertexGlobalDeclaration } from "./clipPlaneVertex-Be6n31Q1.js";
//#region node_modules/@babylonjs/core/Shaders/ShadersInclude/pointCloudVertexDeclaration.js
var name$2 = "pointCloudVertexDeclaration";
var shader$2 = `#ifdef POINTSIZE
uniform float pointSize;
#endif
`;
if (!ShaderStore.IncludesShadersStore[name$2]) ShaderStore.IncludesShadersStore[name$2] = shader$2;
/** @internal */
var pointCloudVertexDeclaration = {
	name: name$2,
	shader: shader$2
};
//#endregion
//#region node_modules/@babylonjs/core/Shaders/ShadersInclude/pointCloudVertex.js
var name$1 = "pointCloudVertex";
var shader$1 = `#if defined(POINTSIZE) && !defined(WEBGPU)
gl_PointSize=pointSize;
#endif
`;
if (!ShaderStore.IncludesShadersStore[name$1]) ShaderStore.IncludesShadersStore[name$1] = shader$1;
/** @internal */
var pointCloudVertex = {
	name: name$1,
	shader: shader$1
};
//#endregion
//#region node_modules/@babylonjs/core/Shaders/depth.vertex.js
var name = "depthVertexShader";
var shader = `attribute vec3 position;
#include<bonesDeclaration>
#include<bakedVertexAnimationDeclaration>
#include<morphTargetsVertexGlobalDeclaration>
#include<morphTargetsVertexDeclaration>[0..maxSimultaneousMorphTargets]
#include<clipPlaneVertexDeclaration>
#include<instancesDeclaration>
uniform mat4 viewProjection;uniform vec2 depthValues;
#if defined(ALPHATEST) || defined(NEED_UV)
varying vec2 vUV;uniform mat4 diffuseMatrix;
#ifdef UV1
attribute vec2 uv;
#endif
#ifdef UV2
attribute vec2 uv2;
#endif
#endif
#ifdef STORE_CAMERASPACE_Z
uniform mat4 view;varying vec4 vViewPos;
#endif
#include<pointCloudVertexDeclaration>
varying float vDepthMetric;
#define CUSTOM_VERTEX_DEFINITIONS
void main(void)
{vec3 positionUpdated=position;
#ifdef UV1
vec2 uvUpdated=uv;
#endif
#ifdef UV2
vec2 uv2Updated=uv2;
#endif
#include<morphTargetsVertexGlobal>
#include<morphTargetsVertex>[0..maxSimultaneousMorphTargets]
#include<instancesVertex>
#include<bonesVertex>
#include<bakedVertexAnimation>
vec4 worldPos=finalWorld*vec4(positionUpdated,1.0);
#include<clipPlaneVertex>
gl_Position=viewProjection*worldPos;
#ifdef STORE_CAMERASPACE_Z
vViewPos=view*worldPos;
#else
#ifdef USE_REVERSE_DEPTHBUFFER
vDepthMetric=((-gl_Position.z+depthValues.x)/(depthValues.y));
#else
vDepthMetric=((gl_Position.z+depthValues.x)/(depthValues.y));
#endif
#endif
#if defined(ALPHATEST) || defined(BASIC_RENDER)
#ifdef UV1
vUV=vec2(diffuseMatrix*vec4(uvUpdated,1.0,0.0));
#endif
#ifdef UV2
vUV=vec2(diffuseMatrix*vec4(uv2Updated,1.0,0.0));
#endif
#endif
#include<pointCloudVertex>
}
`;
if (!ShaderStore.ShadersStore[name]) ShaderStore.ShadersStore[name] = shader;
var includes = [
	bonesDeclaration,
	bakedVertexAnimationDeclaration,
	morphTargetsVertexGlobalDeclaration,
	morphTargetsVertexDeclaration,
	clipPlaneVertexDeclaration,
	instancesDeclaration,
	pointCloudVertexDeclaration,
	morphTargetsVertexGlobal,
	morphTargetsVertex,
	instancesVertex,
	bonesVertex,
	bakedVertexAnimation,
	clipPlaneVertex,
	pointCloudVertex
];
for (const inc of includes) if (!ShaderStore.IncludesShadersStore[inc.name]) ShaderStore.IncludesShadersStore[inc.name] = inc.shader;
/** @internal */
var depthVertexShader = {
	name,
	shader
};
//#endregion
export { depthVertexShader };
