import { _ as ShaderStore } from "./abstractEngine.pure-CkCw37qi.js";
import { t as helperFunctions } from "./helperFunctions-BFr73hb7.js";
//#region node_modules/@babylonjs/core/Shaders/extractHighlights.fragment.js
var name = "extractHighlightsPixelShader";
var shader = `#include<helperFunctions>
varying vec2 vUV;uniform sampler2D textureSampler;uniform float threshold;uniform float exposure;
#define CUSTOM_FRAGMENT_DEFINITIONS
void main(void) 
{gl_FragColor=texture2D(textureSampler,vUV);float luma=dot(LuminanceEncodeApprox,gl_FragColor.rgb*exposure);gl_FragColor.rgb=step(threshold,luma)*gl_FragColor.rgb;}`;
if (!ShaderStore.ShadersStore[name]) ShaderStore.ShadersStore[name] = shader;
var includes = [helperFunctions];
for (const inc of includes) if (!ShaderStore.IncludesShadersStore[inc.name]) ShaderStore.IncludesShadersStore[inc.name] = inc.shader;
/** @internal */
var extractHighlightsPixelShader = {
	name,
	shader
};
//#endregion
export { extractHighlightsPixelShader };
